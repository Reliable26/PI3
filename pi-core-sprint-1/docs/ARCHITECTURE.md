# PI Architecture

Public sources feed connectors. Connectors return standardized events. The engine normalizes events, filters exclusions, scores opportunities, and publishes a public-safe dashboard payload.

Generated intelligence is deployed as a GitHub Pages artifact and is not committed back to the repository.
