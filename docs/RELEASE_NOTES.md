# v0.9.7 Alpha - Inline Property Records

- Replaces the separate detail panel with an inline expandable property record inside the selected property row.
- Removes the need to scroll down to find selected-property information.
- Adds working Collapse control.
- Keeps functional tabs, source-aware permit links, and copy permit number buttons.
- Keeps map below the primary workspace to reduce layout compression.
